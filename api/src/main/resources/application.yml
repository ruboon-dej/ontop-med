llm:
  provider: ${LLM_PROVIDER:openai}   # reads from .env
  openai:
    api-key: ${OPENAI_API_KEY:}
  claude:
    api-key: ${ANTHROPIC_API_KEY:}
  ollama:
    base-url: ${OLLAMA_BASE_URL:http://localhost:11434}